server {
    server_tokens off;

    listen   80;
    listen   [::]:80 default ipv6only=on;

    root /usr/share/nginx/html;

    server_name _;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
